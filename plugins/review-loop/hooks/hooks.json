{
  "description": "Review loop stop hook: runs the selected reviewer for the current round, then has Claude address feedback",
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/stop-hook.sh",
            "timeout": 600,
            "statusMessage": "Review loop: running reviewer..."
          }
        ]
      }
    ]
  }
}
